\documentclass[10pt]{article}
\usepackage[margin=0.85in]{geometry}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage[hidelinks]{hyperref}
\title{Robot-Soccer Transfer: Technical and Experimental Record}
\author{Manually maintained surrogate report}
\date{\today}
\begin{document}
\maketitle

\section{Repository and runtime setup}
Use Python 3.10--3.13, install the pinned editable project, run Ruff and pytest, then execute the
smoke configuration. Every run stores the resolved YAML, source commit, package versions, device,
command, status, seed allocations, and artifact paths. A failed run retains its traceback and an
emergency checkpoint when possible.

The initial Phase 1 notebook completed, but its editable install replaced Colab's Torch, NumPy, and
pandas versions and produced dependency conflicts. The revised notebooks preserve Colab's
CUDA-enabled numerical stack, install only project-specific missing packages at pinned versions,
and install this project editable with \texttt{--no-deps}. They print the effective versions and
fail immediately on installation errors. The current local validation passes all 40 tests.

\section{Detailed environment mechanics}
The field is $9\,\mathrm{m}\times6\,\mathrm{m}$ with a $1.5\,\mathrm{m}$ goal. A macro decision is
repeated for four $0.1\,\mathrm{s}$ low-level steps. The abstract model limits acceleration,
translation, and turn rate; applies linear ball drag; resolves circular contacts; and delivers kick
velocity only inside range and angle tolerances. The transfer model independently creates Pymunk
bodies, force controllers, collision shapes, player-only static containment boundaries, friction,
elasticity, explicit ball drag, and impulse kicks over multiple integration substeps. The ball
crosses field lines for shared terminal classification; players cannot leave through the goal.

Observation and action delay queues are bounded. Object misses set confidence to zero without
changing truth. Packet loss preserves the previous delivered teammate action and increments its
age. The intercept defender protects goal center for a distant ball, tracks a delayed predicted
intercept when threatened, and clears only inside control range.

\section{Full configuration tables}
\begin{longtable}{lll}
\toprule Group & Representative field & Default \\
\midrule
Environment & field / goal & $9.0\times6.0$ m / 1.5 m \\
Environment & policy step & $4\times0.1$ s \\
Environment & episode limit & 250 macro steps \\
Actor & hidden layers & 128, 128 Tanh \\
Local critic & hidden layers & 128, 128 Tanh \\
Central critic & hidden layers & 256, 256 Tanh \\
PPO & $\gamma,\lambda$, clip & 0.99, 0.95, 0.20 \\
PPO & actor/critic learning rate & $3\times10^{-4}$ \\
PPO & epochs / target KL & 5 / 0.03 \\
Training & environments / rollout & 32 / 256 \\
\bottomrule
\end{longtable}
The resolved YAML in each run is authoritative; this table is a readable index, not a replacement.

\section{Reward coefficients}
\begin{tabular}{lr@{\qquad}lr}
\toprule Goal & 10.0 & Defender clear & $-3.0$ \\
Out of bounds & $-3.0$ & Timeout & $-1.0$ \\
Completed pass & 0.5 & Lost/intercepted pass & $-0.1$ \\
Invalid kick/pass & $-0.01$ & Attacker collision & $-0.03$ \\
Time per macro step & $-0.005$ & Progress weight & 1.0 \\
Possession weight & 0.2 & Support weight & 0.15 \\
\bottomrule
\end{tabular}
Progress, possession, and support use potential differences of the form
$\gamma\Phi(s')-\Phi(s)$. Goal reward remains dominant.

\section{Perturbation-profile definitions}
Named profiles cover low, medium, and high action delay; observation corruption at three levels;
communication impairment at three levels; weak/strong kicks; low/high ball friction; slow/fast
movement response; and combined moderate/severe conditions. Reset samples continuous values inside
each YAML range and records both profile and realization. Family-level ablations filter names rather
than changing simulator code.

\section{Curriculum-update derivation}
For success $q_i$, failure $1-q_i$ enters an EMA
$f_i\leftarrow d f_i+(1-d)(1-q_i)$. Weights $(\epsilon+f_i)^\alpha$ are normalized, mixed with
$\beta/N$, then projected onto the capped simplex. Defaults are $d=0.5$, $\epsilon=0.05$,
$\alpha=1$, $\beta=0.1$, and cap 0.25. Curriculum seeds begin at 20,000 and do not update running
normalizers.

\section{PPO implementation notes}
Rollouts are stored as time, environment, agent, feature arrays. MAPPO has one value and one GAE
advantage per environment transition; actor training broadcasts that advantage, while critic
training does not duplicate it. IPPO flattens local value samples across both agents. Truncations
bootstrap from the final observation but stop the recursive advantage chain across reset. Loss and
gradient finiteness is mandatory; a NaN raises after an emergency checkpoint attempt.

\section{Seed allocation}
Training begins at the experiment seed. Ordinary validation and curriculum validation begin at
10,000 and 20,000. The original 30,000/40,000 abstract/transfer ranges were consumed during the
repair investigation and are now explicitly named calibration ranges. New abstract and Pymunk test
ranges begin at 130,000 and 140,000; video scenarios remain at 50,000. Final runs must record every
exact seed and must not reuse model-selection or calibration outcomes as final tests.

\section{Experiment ledger}
The following entries are Phase 1 diagnostics, not final comparative rows.
\begingroup
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{longtable}{p{0.62in}p{0.78in}p{0.62in}p{1.15in}p{0.55in}p{1.35in}p{0.90in}}
\toprule Date & Run name & Commit & Configuration & Seed & Outcome & Decision \\
\midrule
2026-07-21 & smoke test & 3937e9e & smoke test & 0 & complete; artifacts and API path valid & infrastructure only \\
2026-07-21 & baselines & 3937e9e & base; 100 each & 0 & abstract 0.06--0.30; Pymunk 0.73--0.76 & block Phase 2 \\
2026-07-21 & IPPO nominal & 3937e9e & IPPO; 2.0M steps & 0 & learned intercept; transfer non-discriminative & retain; repair benchmark \\
2026-07-21 & repair calibration & working tree & base; 100 each & 30k/40k & mechanics and gate calibration & exclude from final evidence \\
2026-07-21 & repaired baselines & working tree & base; 100 each & 130k/140k & abstract 0.03--0.28; Pymunk 0.49--0.68 & baseline gate passes \\
2026-07-21 & frozen IPPO audit & working tree & best actor; no update & 130k/140k & abstract intercept 0.88; Pymunk 0.43 & conditional Phase 2 go \\
\bottomrule
\end{longtable}
\endgroup

\section{Hyperparameter attempts}
Record each deviation from the checked-in defaults, the validation evidence motivating it, and
whether the choice was made before final-test evaluation. The Phase 1 nominal IPPO run used the
checked-in defaults; no policy hyperparameter selection was performed after its test. Its selected checkpoint
occurred at 1,810,432 environment steps with validation success 0.825 over 40 fixed validation
episodes. The final checkpoint at 2,007,040 steps scored 0.750 on the same-size validation batch,
so the selection rule correctly retained the earlier checkpoint.

The transfer adapter's explicit ball-drag multiplier is 0.78. This benchmark calibration was made
after diagnosing the original reflective-boundary and global-damping defects using the retired
30k/40k ranges. The implementation and launch thresholds were frozen before inspecting the new
130k/140k ranges. This is simulator repair, not a learned-method hyperparameter, but it remains part
of the provenance record.

\section{Initial no-go audit and repair record}
All three restored Phase 1 run records had status \texttt{complete} and no failure exception. A
completed process is not necessarily a valid experiment; the initial audit found the following
problems.
\begin{enumerate}
\item \textbf{Pymunk benchmark saturation.} Random, double-chase, role-based, and nominal-IPPO
success rates are 0.760, 0.730, 0.740, and 0.730. Random and IPPO used the same seeds
40,000--40,099; their paired outcome counts are 58 both-success, 9 both-failure, 15 IPPO-only, and
18 random-only (two-sided exact paired $p=0.728$). The learned actor is therefore not distinguished
from random behavior by the current transfer suite.
\item \textbf{Boundary mismatch.} Abstract baseline out-of-bounds rates are 0.54--0.85, but Pymunk
rates are 0.00--0.01. The abstract simulator terminates a ball beyond the sideline while Pymunk
uses reflective walls. Player bodies can also escape through goal openings; this occurs in the
recorded trajectories. Align or explicitly calibrate these semantics before interpreting a
cross-simulator gap.
\item \textbf{Metric range violation.} Possession is incremented at every physics substep and
divided by macro-action steps. With four repeats it can exceed 1.0 (the observed episode maximum is
3.864). Normalize by executed substeps or count once per macro step, add range assertions, and
regenerate summaries.
\item \textbf{Misnamed minimum.} The standard evaluation has only the nominal profile. Grouping by
profile makes its reported minimum-profile success 0.453, although the minimum across defender
modes is 0.060. Report the defender-mode minimum separately and reserve profile minima for a
profile suite.
\item \textbf{Weak behavioral anchors.} The role-based abstract heuristic succeeds in only 0.060
of episodes and exits out of bounds in 0.85, worse than random success 0.110. The learned policy
makes zero valid pass attempts in 400 final-evaluation episodes and has invalid-action fractions
0.237 on nominal abstract intercept and 0.558 in Pymunk. Inspect action validity and role behavior
before treating goal rate as evidence of cooperative skill use.
\end{enumerate}

The repairs use one terminal classifier after the Pymunk ball crosses a field line, player-only
containment, ball-only explicit drag, target-velocity kick impulses, and bounded speed safeguards.
Possession is sampled once per macro step; invalid attempts divide by two-agent action steps;
summaries explicitly separate defender modes from perturbation profiles; and scripted methods and
defender modes use paired seeds. The role heuristic now transfers the striker role on possession,
uses safer pass and shot conditions, and assigns a narrow loose-ball recovery role. Dedicated tests
cover each repair.

On untouched 130k/140k ranges, all six 100-episode baseline cells pass the coded gate. Random,
double-chase, and role success is 0.03, 0.15, and 0.28 in abstract dynamics and 0.49, 0.68, and 0.66
in Pymunk. Role redundant chasing is 0.029 in Pymunk versus 0.475 for double-chase. The frozen actor
reproduces nominal abstract learning at 0.88 against intercept, but its Pymunk success is only 0.43
versus random's 0.49 (two-sided exact paired $p=0.519$). This negative transfer result is valid and
motivates Phase 2; it is not evidence of physical transfer.

\section{Discarded ideas}
Version one intentionally excludes image observations, joint-level locomotion learning, full 5v5
play, self-play, learned opponents, RNNs, ROS, and physical-deployment claims. Each would add an
independent source of variance or infrastructure before the cross-simulator causal comparison is
established.

\section{Webots setup attempts}
Webots is documented but neither scaffolded nor claimed as tested. Log simulator version, official
NAO/OP3 asset provenance, headless command, motion assets, controller trace, and exact failure here
before changing that status.

\section{Known limitations}
Skill adapters encode task knowledge; the defender is scripted; synchronous collection may limit
throughput; feed-forward actors infer delay only through short observation history; Pymunk remains
a simple planar world; and robustness profiles cannot span unmodeled hardware failures. The frozen
Phase 1 checkpoint generalizes poorly across opponent modes (0.10 against chase-ball versus 0.88
against its trained intercept defender), produces no valid passes in the held-out diagnostic, and
does not exceed random Pymunk success (0.43 versus 0.49). The repaired benchmark is policy-sensitive,
so these are method limitations rather than reasons to modify the test again.

\section{Phase 2 entry gate}
The repaired code and held-out diagnostic satisfy the gate, yielding a conditional go. The gate
requires all of the following and is recomputed by both notebooks.
\begin{itemize}
\item Six complete 100-episode scripted method/simulator cells and bounded fraction metrics.
\item Random Pymunk success at most 0.50 and a scripted-method spread of at least 0.15.
\item Role success above random and role redundant chasing below double-chase in both simulators.
\item Three complete 100-episode defender-mode evaluations, an explicit defender-mode minimum,
and nominal IPPO intercept success of at least 0.50 and above every scripted abstract baseline.
\item A complete 100-episode frozen-policy Pymunk evaluation. Transfer success is measured, not
required to be high; a weak nominal transfer result is the motivation for robustness training.
\end{itemize}

Before spending Phase 2 compute, run the revised Phase 1 notebook once from a clean post-repair
commit and require its \texttt{eval/phase1\_readiness.json} to report
\texttt{phase2\_ready: true}. This final condition gives the fresh actor, configuration, commit,
and all evaluations one coherent provenance record.

\section{Future physical-robot deployment plan}
First validate TorchScript observation parity in a supervised simulator. Then integrate a tested
localization and ball/robot perception stack, collision-safe walking and turning, controlled kick
motions, communication timestamps, action watchdogs, emergency stops, and conservative field
tests. A successful deployment requires logged physical trials, not merely controller startup.

\section{Reproducibility checklist}
\begin{itemize}
\item Record clean commit, resolved YAML, packages, device, and all seed ranges.
\item Pass Ruff, the full test suite, both PettingZoo API checks, and the smoke command.
\item Preserve complete checkpoints, normalizers, curriculum history, CSV metrics, and logs.
\item Evaluate frozen checkpoints on fixed abstract and Pymunk seeds without retraining.
\item Aggregate multiple seeds and report variability, tail metrics, and failed runs.
\item Rebuild both reports from restored Drive artifacts in a fresh Colab runtime.
\item Describe Pymunk as sim-to-sim and do not infer physical-robot performance.
\end{itemize}
\end{document}
